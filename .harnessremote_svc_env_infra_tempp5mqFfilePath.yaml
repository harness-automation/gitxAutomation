template:
  name: remote_svc_env_infra_tempp5mqF
  identifier: remote_svc_env_infra_tempp5mqF
  versionLabel: v1
  type: Stage
  projectIdentifier: NGPipeAutoPipelineSvcPodUfSK16G
  orgIdentifier: Pipelines_NonK8s_Org_NG
  tags: {}
  spec:
    type: Deployment
    spec:
      deploymentType: Kubernetes
      service:
        serviceRef: NGPipeAutoSvcsD1vPF57si
        serviceInputs:
          serviceDefinition:
            type: Kubernetes
            spec:
              artifacts:
                primary:
                  sources:
                    - identifier: artifactId
                      type: Gcr
                      spec:
                        tag: "80"
      environments:
        metadata:
          parallel: true
        values:
          - environmentRef: NGPipeAutoeF1Pi5pT5t
            deployToAll: false
            infrastructureDefinitions:
              - identifier: NGPipeAutoFIRFW3KtI3
                inputs:
                  identifier: NGPipeAutoFIRFW3KtI3
                  type: KubernetesDirect
                  spec:
                    namespace: default
      execution:
        steps:
          - step:
              name: Rollout Deployment
              identifier: rolloutDeployment
              type: K8sRollingDeploy
              timeout: 10m
              spec:
                skipDryRun: false
                pruningEnabled: false
        rollbackSteps:
          - step:
              name: Rollback Rollout Deployment
              identifier: rollbackRolloutDeployment
              type: K8sRollingRollback
              timeout: 10m
              spec:
                pruningEnabled: false
    failureStrategies:
      - onFailure:
          errors:
            - AllErrors
          action:
            type: StageRollback
